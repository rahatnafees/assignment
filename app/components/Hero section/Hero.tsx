import styles from './hero.module.css';

const Hero=() => {
    return(
        <section className={styles.hero}>
            <h2>Welcom to the Hero Section</h2>
            <p> </p>
        </section>
    )
};
export default Hero;